# Database for all contigs already made, external-genomes.txt file already made

for i in *fa
do
    # use basename for easier file naming
    base=$(basename $i .fa)
    
    anvi-get-sequences-for-hmm-hits -c $i --hmm-source coldshock_hmm
    -o ${base}-sequences-for-hmm-hits.fa

done
